---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tvheadend-config
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-csi
