---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tvheadend-config-v2
  namespace: kube-system
spec:
  storageClassName: nfs-csi
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 1Gi
